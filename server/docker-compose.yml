services:
  api:
    image: registry.ham-san.net/the-sorter-server:latest
    restart: unless-stopped
    env_file: .env
    environment:
      PORT: 3000
    ports:
      - '127.0.0.1:${API_PORT:-3100}:3000'
