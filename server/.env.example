DATABASE_URL=postgres://sorter:sorter@localhost:5433/sorter
PORT=3000
CORS_ORIGINS=https://hamproductions.github.io,http://localhost:3000,http://localhost:5173
ADMIN_TOKEN=
# Header the reverse proxy sets with the visitor IP (e.g. x-forwarded-for; the last entry is used).
# Leave empty only when the server is reached directly, otherwise all visitors share one rate limit.
CLIENT_IP_HEADER=
DATA_BASE_URL=https://raw.githubusercontent.com/hamproductions/the-sorter/main/data
