import type { Config } from 'vike/types';

export default {
  title: 'Marking Mode',
  // Disable prerendering for this dynamic route - IDs are user-generated at runtime
  prerender: false
} satisfies Config;
